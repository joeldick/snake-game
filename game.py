"""DEPRECATED: Desktop Python game removed.

This file is retained as a placeholder so the repository history remains intact.
The active web implementation lives in `src/` (see `src/game.js`).
"""

raise SystemExit("Desktop Python game removed — use the web version (npm run dev)")