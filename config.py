"""DEPRECATED: Desktop Python configuration removed.

This placeholder remains to preserve repository history. The web
version uses different configuration patterns located in `src/`.
"""

raise SystemExit("Desktop Python config removed — use the web version (npm run dev)")